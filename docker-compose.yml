services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: hrtool
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - hrtool_postgres_data:/var/lib/postgresql/data

volumes:
  hrtool_postgres_data:
